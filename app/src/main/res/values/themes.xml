<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TabbedLayout" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.TabbedLayout.AppBarOverlay" parent="ThemeOverlay.AppCompat.DayNight.ActionBar"/>

    <style name="Theme.TabbedLayout.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="EstiloTabBar" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#FFF</item>
        <item name="tabTextColor">#FFF</item>
        <item name="tabIndicatorHeight">5dp</item>
        <item name="tabPaddingEnd">8dp</item>
        <item name="tabPaddingStart">8dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabSelectedTextColor">#1f2114</item>
    </style>
</resources>